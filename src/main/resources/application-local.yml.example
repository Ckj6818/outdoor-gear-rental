# 本地 MySQL 配置（复制为 application-local.yml 并填写真实密码）
# Spring Boot 会自动加载 application-local.yml（若存在）
spring:
  datasource:
    username: root
    password: 123456

# AI 导购 API Key（也可通过环境变量 AI_API_KEY 注入）
ai:
  consultant:
    api-key: your-deepseek-api-key
